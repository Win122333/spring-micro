spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    password: qewr
    username: postgres
    url: jdbc:postgresql://localhost/orders
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true


server:
  port: 8082